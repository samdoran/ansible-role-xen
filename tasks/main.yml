---
- name: Install packages
  yum: name={{ item }} state=present enablerepo=base
  with_items:
    - yum-utils
    - vim-enhanced
  tags: [ 'xen' , 'yum' ]

- include: ntp.yml
- include: ssh.yml
- include: sudo.yml
- include: harden.yml
